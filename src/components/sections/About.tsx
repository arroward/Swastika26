'use client';

import SlantedMarquee from '@/components/SlantedMarquee';

export default function About() {
    return (
        <section className="h-screen w-full relative overflow-hidden bg-black/20 backdrop-blur-sm">
            <SlantedMarquee />
        </section>
    );
}
